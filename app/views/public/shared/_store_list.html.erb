<!--店舗一覧・店舗検索結果ページで使うパーシャル-->

<div class="row">
  <% if stores.any? %>
    <% stores.each do |store| %>
      <div class="col-md-5 mb-4">
        <% if store.images.attached? %>
          <%= link_to store_path(store) do %>
            <%= image_tag store.images.first.variant(resize_to_limit: [600,400]), class:"img-fluid" %>
          <% end %>
        <% else %>
          <p>画像がありません</p>
        <% end %>
      </div>

      <div class="col-md-7 mb-5">
        <p>店舗名：<strong><%= store.name%></strong></p>
        <p>投稿者：<%= store.user.name %></p>
        <p>評価：<div id="score_<%= store.id %>"></div></p>
        <p>カテゴリ：<%= store.category.name %></p>
        <p>
          <% store.tags.each do |tag| %>
            <span>タグ：<%= tag.name %></span>
          <% end %>
        </p>
        <p>レビュー：<%= truncate(store.description, length: 100) %></p>
        <p><%= link_to "この店舗の詳細をみる", store_path(store) %></p>
      </div>
      <script>
        $('#score_<%= store.id %>').raty({
          starHalf: '<%= asset_path("star-half.png") %>',
          starOff: '<%= asset_path("star-off.png") %>',
          starOn: '<%= asset_path("star-on.png") %>',
          score: '<%= store.score %>',
          readOnly: true
        })
      </script>

    <% end %>
  <% else %>
    <p>まだ投稿がありません</p>
  <% end %> 
</div>